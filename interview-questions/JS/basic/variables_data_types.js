//1. Declare a variable price and assign it a floating value of your choice.
var price = 10.5;
console.log(price);

//2. Declare a variable name and assign it a string value of your choice.
var name = "John";
console.log(name);

//3. Declare a variable fullName and concatenate two strings to form a full name.

var firstName = "John";
var lastName = "Doe";
var fullName = firstName + " " + lastName;

//4. Use typeof to check the type of a variable score with the value 100.

var score = 100;
console.log(typeof score);

//5. Declare a variable length and assign it a value of 16.5 and check if it is an integer.

var length = 16.5;
console.log(Number.isInteger(length));
